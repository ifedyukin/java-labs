message.default.locale = Default locale: US
message.origin = Origin array:
message.worked = Array with changed values between min and max equal difference min and max:
message.noResult = (Max and min elements are near)